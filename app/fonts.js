import { Baloo_2, Roboto, Oswald } from 'next/font/google';

export const baloo2 = Baloo_2({
  weight: ['400', '700', '800'],
  subsets: ['latin'],
});

export const roboto = Roboto({
  subsets: ['latin'],
  weight: ['400', '700'],
});
